import {
    REGISTRO_EXITO,
    REGISTRO_ERROR,
    OBTENER_USUARIO,
    LOGIN_EXITO,
    LOGIN_ERROR,
    LOGOUT
} from '../../types';

export default (state, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}